export default class Hex {
    q: number;
    r: number;
    x: number;
    y: number;
    constructor(q: number, r: number, x: number, y: number) {
        this.q = q;
        this.r = r;
        this.x = x;
        this.y = y;
    }
}